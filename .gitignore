# data, outputs and logs are regenerated by run_all.R (sources are public)
data/
output/
logs/
*.nc4
# local scratch
_to_delete_/
.Rhistory
.RData
.Rproj.user/
.DS_Store
# renv library (commit renv.lock and renv/activate.R)
renv/library/
renv/staging/

# unpublished manuscript drafts (remove this line to version them)
manuscript/

# files the desktop app saves into the project
Claude outputs/
